# ── Local dev (SQLite, no server needed) ────────────────────────
SECRET_KEY=changeme-dev-only-32chars-minimum!
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
ML_MODEL_REGISTRY=mlruns

# ── PostgreSQL (local Docker or remote DB) ───────────────────────
# POSTGRES_URL=postgresql+asyncpg://user:pass@localhost:5432/ai_learning
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/ai_learning

# ── Production (Render / Railway / Heroku) ───────────────────────
# SECRET_KEY=<generate: python -c "import secrets; print(secrets.token_hex(32))">
# POSTGRES_URL=postgresql+asyncpg://<user>:<pass>@<host>:5432/<db>
# REDIS_URL=redis://<host>:6379/0
# CELERY_BROKER_URL=redis://<host>:6379/1
