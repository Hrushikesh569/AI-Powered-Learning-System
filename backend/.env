# Local dev environment (SQLite — no DB server needed)
# For Docker, these are overridden by docker-compose.yml environment: section
SECRET_KEY=changeme-dev-only-32chars-minimum!
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
ML_MODEL_REGISTRY=mlruns
# Uncomment and set for PostgreSQL local dev:
# POSTGRES_URL=postgresql+asyncpg://user:pass@localhost:5432/ai_learning
